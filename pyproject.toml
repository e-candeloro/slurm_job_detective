[project]
name = "slurm-job-detective"
version = "0.1.0"
description = "A simple CLI tool to see if your SLURM Jobs state and resource usage."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
